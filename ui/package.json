{
  "name": "core-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "NUI shell for the core framework (Vue 3 + Vite, builds to ../html)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "storybook": "storybook dev -p 6006 --no-open",
    "build-storybook": "storybook build -o storybook-static",
    "build:server": "esbuild ../server/pg/index.js --bundle --platform=node --target=node22 --format=cjs --external:pg-native --banner:js=\"// fxlint-disable-file: generated by 'npm run build:server' (core/ui) from server/pg/index.js - edit the source, not this file.\" --outfile=../server/db_pg.js --log-level=warning"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@storybook/addon-docs": "^10.6.0",
    "@storybook/vue3-vite": "^10.6.0",
    "@tailwindcss/vite": "^4.3.3",
    "@vitejs/plugin-vue": "^6.0.0",
    "esbuild": "^0.25.12",
    "pg": "^8.23.0",
    "remark-gfm": "^4.0.1",
    "storybook": "^10.6.0",
    "tailwindcss": "^4.3.3",
    "vite": "^7.0.0"
  }
}
